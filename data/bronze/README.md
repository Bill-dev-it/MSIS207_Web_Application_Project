# Bronze

Original source bytes are preserved. Do not clean or edit datasets here.
Use `../bronze_manifest.csv` to verify files against pre-migration SHA-256 hashes.

See `../README.md` for source provenance. The `faa` bucket includes separately
labeled third-party aircraft reference sources, not only FAA-issued data.
NASA C-MAPSS is not present yet; `cmapss/` is a placeholder.
